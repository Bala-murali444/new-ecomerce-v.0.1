node {
    checkout scm
    docker.withRegistry('https://registry.hub.docker.com','docker-hub-ecom') {
        def customImage = docker.build('9885614249/ecom-app')
        customImage.push()

    }
}
